# Security Policy

## 教材としての安全範囲

このプロジェクトは、Windows標準のDNSリゾルバーへ読み取り専用の問い合わせを行う学習用サンプルです。DNSレコードの登録・更新・削除、管理者権限の取得、外部コマンドの起動は行いません。

## 実行前の注意

- マクロは内容を確認し、信頼できる学習用ブックへ取り込んでください。
- 学校・会社では、外部DNS問い合わせに関するネットワークポリシーへ従ってください。
- 機密性の高い社内ホスト名や未公開ドメインを、Issueやスクリーンショットへ掲載しないでください。
- 不審な第三者製ブックへこのコードを組み込まないでください。

## 実装上の防御

- `Shell`、`WScript.Shell`、PowerShell、`nslookup.exe`を使用しません。
- `DnsQuery_W`が返したポインターだけを読み取り対象にします。
- レコード数、TXT文字列数、文字列長、データ長に安全上限を設けています。
- 連結リストが自分自身を指す場合を検出します。
- 成功後のレコードリストは、解析エラー時を含めて必ず解放します。
- Traceおよび結果シートの文字列列をテキスト形式にし、DNS由来の文字列がExcel数式として評価されることを防ぎます。
- 改行、タブ、巨大な文字列はトレース表示用に無害化・省略します。

## 残るリスク

VBAからネイティブポインターを扱うため、構造体オフセットやAPI宣言の誤りはExcelの異常終了につながる可能性があります。実運用システムへ転用する前に、対象となるOfficeの32bit/64bit環境で十分にテストしてください。

DNSの回答は信頼済みデータとは限りません。本サンプルは値を表示するだけであり、回答に含まれるホスト名やTXT値をコマンド、URL、数式として自動実行しません。派生作品でも、自動実行へ直接渡さないでください。

## 脆弱性の報告

公開Issueへ機密情報を含めず、再現条件、Officeのbit数、Windowsのバージョン、該当するレコード種類を必要な範囲で示してください。クラッシュを報告する場合は、実在する社内名を`example.com`などの予約済み例示名へ置き換えてください。
